<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.custalarmable">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/color_change" />

        <activity
            android:name=".Analysis"
            android:label="@string/analysis_name"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DefaultSetting"
            android:label="@string/analysis_name"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AlarmSetting"
            android:label="@string/analysis_name"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SnoozeSetting"
            android:label="@string/analysis_name"
            android:parentActivityName=".AlarmSetting" />
        <activity
            android:name=".Sleep"
            android:label="@string/analysis_name"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Work"
            android:label="@string/analysis_name"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>